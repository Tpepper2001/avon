CREATE TABLE users (id INTEGER PRIMARY KEY, email TEXT UNIQUE, password TEXT, voxKey TEXT UNIQUE);
CREATE TABLE messages (id INTEGER PRIMARY KEY, voxKey TEXT, videoUrl TEXT, timestamp INTEGER);
CREATE INDEX idx_key ON messages(voxKey);
